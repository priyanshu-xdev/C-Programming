Compute the power of a number using a loop (e.g., a^b).

#include <stdio.h>
int main()
{
    int a, b, c = 1;
    printf("Enter Base:");
    scanf("%d", &a);
    printf("Enter Power:");
    scanf("%d", &b);
    for (int i = 1; i <= b; i++)
    {
        c = c * a;
    }
    printf("Result=%d", c);
}
