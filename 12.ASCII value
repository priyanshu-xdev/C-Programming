Create a program that reads a character and prints its ASCII value.

#include <stdio.h>
int main()
{
    char ch;
    printf("Enter a Character:");
    scanf("%c", &ch);
    printf("ASCII value of %c is %d", ch, ch);

    return 0;
}
